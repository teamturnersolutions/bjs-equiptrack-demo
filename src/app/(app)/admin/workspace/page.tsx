import WorkspaceWrapper from './workspace-wrapper';

export const metadata = {
  title: "Administrative Workspace | BJ's EquipTrack",
};

export default function WorkspacePage() {
  return <WorkspaceWrapper />;
}
